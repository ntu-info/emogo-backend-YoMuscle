[pytest]
# 測試設定
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# 非同步設定
asyncio_mode = auto

# 輸出設定
addopts = -v --tb=short

# 標記定義
markers =
    slow: 標記為慢速測試
    integration: 標記為整合測試
